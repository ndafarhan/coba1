1. git clone <link>
(untuk download repository ke local, ini berlaku jika belum ada folder di local. jika sudah ada repo di lokal langsung akses via terminal)
2. git config --list
(untuk melihat akun github)
3. git pull origin master
(mengupdate local berdasarkan last commit repo)
4. git add .
(untuk add semua file, bisa jg . diganti dg file spesifik)
5. git commit -m "kata2nya"
(untuk menambahkan keterangan)
6. git push
(untuk upload ke repository github)

jika ingin upload ke repository lain bisa langsung ganti directory via terminal dan ulangi step di atas
